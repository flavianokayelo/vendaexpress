// Seed de theme_config para lojas novas — versão do tema "Standard Ecommerce".
// Os valores reais dos tokens vivem no frontend (src/theme/defaultTheme.ts);
// isto é só um marcador de versão para permitir, no futuro, temas alternativos.
const DEFAULT_THEME_CONFIG = JSON.stringify({ themeVersion: 'standard-ecommerce-v1' });

module.exports = { DEFAULT_THEME_CONFIG };
